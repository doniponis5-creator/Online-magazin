-- ============================================================================
-- Интернет-магазин Smart Centr: остаток по рассрочке для чата на сайте.
-- Только новая таблица; существующие (installment_payments и др.) не затрагиваются.
--
-- 1С присылает полный снимок раз в 10 минут: кто сколько должен, когда
-- ближайший платёж и сколько месяцев осталось. Одна строка — один телефон.
-- Сайт спрашивает только по телефону ВОШЕДШЕГО покупателя (код подтверждён),
-- поэтому по чужому номеру отсюда ничего не узнать.
-- ============================================================================

CREATE TABLE IF NOT EXISTS shop_installments (
    phone       VARCHAR(20) PRIMARY KEY,         -- +996XXXXXXXXX или +7XXXXXXXXXX
    data        JSONB       NOT NULL,            -- долг, ближайший платёж, покупки
    updated_at  TIMESTAMP   NOT NULL DEFAULT NOW()
);
