-- ============================================================================
-- Интернет-магазин Smart Centr: «Знания для чата» — текст владельца.
-- Только новая таблица; существующие не затрагиваются.
--
-- Владелец пишет в 1С («Панель сайта») обычными словами: часы работы, гарантия,
-- возврат, акции. Чат на сайте и в Telegram читает этот текст и отвечает по нему.
-- Одна запись: текст целиком, как его сохранили.
-- ============================================================================

CREATE TABLE IF NOT EXISTS shop_assistant_notes (
    id          INTEGER PRIMARY KEY DEFAULT 1 CHECK (id = 1),
    text        TEXT        NOT NULL DEFAULT '',
    updated_at  TIMESTAMP   NOT NULL DEFAULT NOW()
);
