/**
 * Фотографии товаров в витрине временно НЕ используются (решение владельца
 * от 12.09): реальные модели и снимки предоставит владелец после согласования.
 * В товарных местах интерфейса — единый нейтральный плейсхолдер
 * («Фото скоро появится»). Фотография остаётся только в hero-баннере —
 * она подключена статическим импортом в Hero3D (src/assets/hero-blue-lime.jpg),
 * поэтому смена файла меняет URL и кеш next/image не отдаёт старую копию.
 *
 * Тип оставлен для будущих реальных снимков и для тестовой фикстуры зума
 * (/dev/gallery?fixture=zoom), которая не входит в публичную витрину.
 */
export type ProductPhoto = {
  src: string
  altRu: string
  altKy: string
  /** краткая атрибуция автора снимка */
  credit: string
}
