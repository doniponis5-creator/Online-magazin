# TASK 04 — отчёт: Blender-модель LG F4X5ES5SB (внешний корпус) + 2 рендера

Дата: 12.09.2026. Ветка: `feature/milestone-01-02-storefront`.
Задание: `TASK_04_LG_REALISTIC_FRAMES.md` + уточнение владельца 12.09:
официальные фото — только референс моделирования; исправить пустой
contact-sheet и неверные аннотации; один Blender-источник (внешний
корпус, панель, дверца, реальные материалы), из него — фронт и 3/4;
внутренний механизм, вода и скролл — НЕ на этом этапе; фото рендером
не сдавать, сходство сверять с официальным референсом.

## Что сделано

1. **Референсы переупорядочены.** Официальные фото переехали в
   `assets-src/lg-f4x5es5sb/references/` и используются только как
   референс моделирования. Прежние «кадры из фото» (01/02/03, кривые
   аннотации) удалены из сдачи — аннотированный внутренний кадр снят
   полностью (внутренний механизм отложен владельцем на следующий этап).
2. **Исправлен пустой contact-sheet.** Причина найдена: SVG-оверлей
   содержал непрозрачный белый `<rect>` на весь холст и при композите
   ложился ПОВЕРХ фотографий — лист выглядeл пустым (видны были только
   подписи). Исправлено: оверлей прозрачный (только текст), кадры
   кладутся на белый холст под ним. Новый `contact-sheet.png` собран из
   двух РЕНДЕРОВ (не фото) и проверен глазами — оба кадра видны.
3. **Blender-модель (один источник).** `build_model.py` (процедурный,
   воспроизводимый; Blender 5.2.1 LTS headless) строит ТОЛЬКО внешний
   корпус: серебристый корпус 600×850×560 с фасками, чёрная верхняя
   панель с центральным хром-переключателем (+метка), дисплей слева
   (лёгкая эмиссия), 4 кнопки справа, крупная дверца (хром-кольцо +
   тёмное стекло + ручка), выдвижной лоток слева, сервисная крышка
   внизу справа, тёмные швы, 4 ножки. Материалы: сатин-металл
   (анизотропия), хром, тёмное стекло, пластик; студийный свет
   (ключевой AREA + заполняющий + деликатные синий/лаймовый акценты
   бренда), светлый пол, контактные тени. Сохранён
   `lg-f4x5es5sb.blend` — единая сцена для будущих этапов.
4. **Два рендера из одной модели** (не фото): `01-front.png`,
   `02-three-quarter.png` — 1600×1200, Cycles, 192 сэмпла + денойз.
   Камеры: фронт (66 мм) и ¾ (62 мм, видны фронт + бок + глубина).
5. **Итерация по итогам осмотра.** После первого рендера модель
   доработана: ярче хром (roughness 0.06), крупнее и мягче ручка
   дверцы, заметнее лоток моющего средства (выступ + ручка), добавлен
   тёмный шов под панелью. Рендеры перевыполнены из того же скрипта.
6. **Сверка с официальным референсом.** Собран
   `compare-with-official.jpg` (наш рендер ↔ официальное фронтальное
   фото рядом) и осмотрен: силуэт, компоновка панели/дверцы/лотка/
   крышки и общие пропорции узнаваемо совпадают; заявляем
   «согласованную стилизованную модель с реалистичными материалами»,
   НЕ фотореализм. Честные ограничения: точные кривизны фасок и оттенки
   — наша интерпретация; логотипы/шрифты панели не воспроизводимы.

## Сдано (`review/task-04/`)

| Файл | Размер | Что это |
|---|---|---|
| `01-front.png` | 1600×1200, ~1.8 МБ | Фронтальный рендер из Blender-модели |
| `02-three-quarter.png` | 1600×1200, ~1.8 МБ | Ракурс 3/4 из той же модели |
| `contact-sheet.png` | 1550×612, ~0.5 МБ | Оба рендера рядом (исправленный лист) |
| `compare-with-official.jpg` | 1430×583, ~45 КБ | Рендер ↔ официальное фото (сверка) |
| `MODEL_SOURCES.md` | — | Источник, референсы, допущения |

Исходники: `assets-src/lg-f4x5es5sb/` — `build_model.py` (~10 КБ),
`lg-f4x5es5sb.blend` (~1.4 МБ), `references/` (6 официальных фото,
~1 МБ — теперь только референс). Blender (405 МБ) в `.tools/`, в git
не входит.

## Границы этапа (честно)

- Внутренний механизм (барабан/DD), вода и привязка к скроллу — НЕ
  делались по указанию владельца; сцена готова к их добавлению в .blend.
- Логотипы LG и надписи панели не моделировались (права/точность
  шрифтов); при необходимости — отдельное решение владельца.
- Код магазина не менялся; набор его тестов не запускался (не требуется
  по заданию).

Останавливаюсь на дизайн-ревью двух рендеров и модели.
