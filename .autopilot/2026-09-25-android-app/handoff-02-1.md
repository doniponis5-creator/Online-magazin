СДЕЛАНО: истории 10 (карта офлайн, QR, sbrt=1.0) и 13 (без отпечатка — честная строка) пройдены; 12 — «Включён» и «Проверить» → «Сработало» пройдены, «Войти по отпечатку» появляется, отпечаток открывает ключ, но сервер ответил 401 (см. ТУПИКИ). Не сделаны: 12 (конец), 11, 5, 6, итоговый AAB, jarsigner, копия в build-play/smarket-1.0-code3.aab, docs/ANDROID_PLAY_UZ.md. Лог — build-play/android-check/02/check.log, снимки там же, logcat-02-part1.txt без FATAL.
ФАЙЛЫ:   android/app/build.gradle — versionCode 3 (готово, оставить). capacitor.config.ts и AndroidManifest.xml возвращены к исходным байт в байт, cap sync сделан, в assets адрес https://smarket.kg. vitest 249 passed. Помощник adb — build-play/android-check/02/h2.sh (source; в нём tapt, dump, reach, login, air, top, shot).
РЕШЕНИЯ: временная правка для проверки — три строки: в capacitor.config.ts server.url http://10.0.2.2:3100 и cleartext true; в AndroidManifest.xml у application android:usesCleartextTraffic="true" (Capacitor 8 сам http не разрешает — без этого WebView не грузит). Всё три вернуть до bundleRelease. next dev -p 3100 работает (allowedDevOrigins уже включает 10.*). Вход: +996700123456, код 1234; новый номер просит имя.
ТУПИКИ: конец сессии перезапуском next dev не годится — тестовые клиенты живут в памяти сервера, после перезапуска native-key отвечает 401 (route.ts стр. 26), и сайт стирает ключ. Кнопки после клавиатуры жать только после скрытия клавиатуры (тап по заголовку), иначе тап попадает в «Корзину». «Войти» на форме — через Enter. Эмулятор грузится из снимка: PIN и отпечаток, скорее всего, пропали — заново: locksettings set-pin 1111, am start -a android.settings.FINGERPRINT_ENROLL, PIN 1111+Enter, MORE×2, I AGREE, 14× emu finger touch 1, DONE.
ДАЛЬШЕ: временная правка → cap sync → assembleRelease → вход → конец 12: закончить сессию, не трогая сервер (например settings put global auto_time 0 и cmd alarm set-time на +181 день — cookie 180 дней, ключ 365) → «Войти по отпечатку» впускает; затем 11 («Выйти» → офлайн без «Бонусная карта»), 5 (заказ → тестовая оплата → возврат), 6 (adb push картинки + сканирование → чат → превью, не отправлять). Потом вернуть правку, bundleRelease, jarsigner (алиас smarket-upload), копия AAB, docs.
