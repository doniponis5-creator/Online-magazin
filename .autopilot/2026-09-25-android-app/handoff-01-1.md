СДЕЛАНО: AVD smarket-api36 создан и грузится; cap sync + assembleRelease собираются, APK ставится. Пройдены истории 1–6 (O!Деньги → таск 02), 9, 13 (падения нет; строку «быстрого входа нет» видно только после входа → таск 02). Истории 7–8: своя страница «Нет связи» есть (запуск офлайн и переход). НЕ закрыто: история 14 — на странице «Нет связи» кнопка «Каталог» не появляется. Весь лог — build-play/android-check/01/check.log, снимки там же. logcat: FATAL/AndroidRuntime от kg.smarket.app нет.
ФАЙЛЫ: код не менялся (git чистый в android/, capacitor.config.ts, ios-web/). AVD создан вручную: ~/.android/avd/smarket-api36.ini и smarket-api36.avd/config.ini (avdmanager из brew не видит SDK — «Package path is not valid»). На эмуляторе стоит чистый release-APK (versionCode 2), эмулятор выключен.
РЕШЕНИЯ: снимок каталога сохраняется — проверено диагностической сборкой (в android/app/src/main/assets/capacitor.config.json временно loggingBehavior=production → logcat «Capacitor/Plugin … OfflineCatalog save», ответ resolved). После проверки конфиг возвращён через `npx cap sync android`. Release не отлаживается (run-as и devtools закрыты) — диагностика только так. Цвет заставки #FEF102 ≠ фон иконки #EAF500 (ic_launcher_background.xml) — не менял, решение за следующим: история 2 требует «в цветах иконки», на iOS заставка — картинка Splash.imageset.
ТУПИКИ: `uiautomator dump` внутри WebView зависает (ждёт idle) — не звать, пока открыт сайт; жать по координатам со снимков (снимок 1080×2400; уменьшенный sips -Z 1000 → множитель 2.4). Вкладки «Каталог»/«Кабинет» офлайн отдаются из кэша Next.js (без страницы «Нет связи»; «Кабинет» пишет «Сервис временно недоступен» — это сайт). Надёжный вход в «Нет связи»: am force-stop + запуск без сети. В macOS нет PIL и timeout.
ДАЛЬШЕ: история 14. На https://localhost/index.html (errorPath) скрипт страницы не вызывает OfflineCatalog.state — в logcat нет ни одного «To native … state», зато есть «Error injecting safe area CSS: Cannot read properties of null (reading 'style')» из JS Capacitor (SystemBars, до появления DOM). Проверить: есть ли window.Capacitor.Plugins.OfflineCatalog на локальной странице (в Capacitor 8 устаревший Capacitor.Plugins может не заполняться; на smarket.kg работает). Вероятная правка — в ios-web/index.html звать через Capacitor.nativePromise('OfflineCatalog','state',{}) / после готовности моста, не ломая iOS; затем cap sync, пересборка, история 14 (поиск, раздел, «Закрыть»), «Повторить» после возврата сети (история 8), итоговый vitest (сейчас 249 passed).
