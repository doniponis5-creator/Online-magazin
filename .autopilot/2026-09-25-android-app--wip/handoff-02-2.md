СДЕЛАНО: история 12 пройдена до конца (конец сессии → «Войти по отпечатку» → finger touch → POST native-key 200 → /me 200, в кабинете «Test Tester»; снимки s12r2-*); история 11 пройдена («Выйти» → DELETE /me 200 → офлайн «Нет связи» без «Бонусная карта»; s11-logged-out, s11-offline-no-card). Не сделаны: 5, 6 (наполовину), итоговый AAB, jarsigner, копия build-play/smarket-1.0-code3.aab, docs/ANDROID_PLAY_UZ.md. logcat-02-part2.txt — FATAL от kg.smarket.app нет. vitest 249 passed.
ФАЙЛЫ:   android/app/build.gradle — versionCode 3 (оставить). capacitor.config.ts и AndroidManifest.xml возвращены байт в байт (git diff android/ = только build.gradle), cap sync сделан, в assets https://smarket.kg. APK в android/app/build/outputs/apk/release/ — ещё временный (http://10.0.2.2:3100), для итога не годится. Помощники: build-play/android-check/02/h2.sh и r2.sh (source оба; r2: reach2, tapmid, cab, login3). Лог — check.log там же.
РЕШЕНИЯ: временная правка та же, что в handoff-02-1 (3 строки). Конец сессии без трогания сервера: settings put global auto_time 0 и cmd alarm set-time на +181 день, потом вернуть set-time на текущее время — работает. После загрузки эмулятора с PIN телефон заблокирован и am start пишет «Activity does not exist»: сначала разблокировать (keyevent 224, свайп вверх, input text 1111, Enter). Отпечаток и PIN сохранились в AVD. После кода поле имени уже в фокусе: печатать сразу и Enter, не тапать. air() принимает enable/disable, не 1/0. Координаты нижнего меню: Каталог 324,2220; Корзина 540,2220; Кабинет 972,2220; кнопка чата 972,2061; «Прислать фото товара» 86,2176.
ТУПИКИ: reach() из h2.sh ловит поле в полосе 300..1600 — там липкая строка поиска, номер уходит в поиск; брать reach2/login3 из r2.sh (полоса 600..1700). «В корзину» на главной и в /ru/catalog в uiautomator имеют y=2339/2320 (схлопнуты) — тап по тексту не работает; открыть карточку товара и жать «В корзину» там, либо по снимку и координатам. В Photo Picker первая плитка — большой снимок экрана → сайт «Фото слишком большое» (значит файл дошёл до страницы; лимит — сайтовый); нужная картинка — s6-test-photo.png, 13 КБ, в /sdcard/Pictures, в MediaStore есть.
ДАЛЬШЕ: временная правка → cap sync → assembleRelease → install → разблокировать → история 5 (карточка товара → «В корзину» → Корзина → форма → заказ; в тестовом режиме payUrl — своя страница /ru/order/TEST-… с кнопкой тестовой оплаты → нажать → «оплачен», приложение живо; снимок) → история 6 (выбрать s6-test-photo.png через «Albums»/Pictures, превью, НЕ отправлять) → вернуть правку, cap sync → bundleRelease → jarsigner (алиас smarket-upload) → assets/capacitor.config.json в AAB → копия → docs (дата 25.09.2026, следующий versionCode 4) → vitest.
