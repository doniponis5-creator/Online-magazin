# dependencies
node_modules/

# next.js build output
.next/
out/

# generated types
next-env.d.ts
*.tsbuildinfo

# misc
.DS_Store
*.pem

# env files — keep credentials out of git
.env
.env.*
!.env.example

# logs
npm-debug.log*
*.log

# playwright
test-results/
playwright-report/
.tools/
scripts/1c-export/output/
integrations/1c-online-shop/build/
smartcentr-site.tar.gz

__pycache__/
*.pyc

# папки агентов — у каждого свои, в общий репозиторий не идут
.agent/
.agents/
# .claude/ — своя у каждого, КРОМЕ навыков: они общие для всех компьютеров
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/caveman/
.codex/
.cursor/
.gemini/
.zcode/

# карта проекта (graphify): в git идёт только граф и отчёт.
# Всё остальное каждый компьютер строит сам за пару минут.
graphify-out/graph.html
graphify-out/cache/ast/
graphify-out/cache/stat-index.json
graphify-out/cache/last_query_stamp
graphify-out/cost.json
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/20*/
build-appstore/
build-play/

# Журнал вопросов из чата — только на сервере, в git не нужен
data/
