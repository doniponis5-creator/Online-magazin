# Smart Centr — что сделать дальше

> Сначала прочитайте [HANDOFF.md](HANDOFF.md) — там правила работы и что уже готово.
> Дата: 2026-09-17. Порядок задач = порядок выполнения.

## ⚠ Прочти это первым, PC (написано 2026-09-21 с MacBook)

**1. Сайт уже выкачен, на нём новое.** На `smarket.kg` работает онлайн-консультант
(чат в углу) и телеграм-бот @ssmartket_bot. Что это и где лежит — `HANDOFF.md`
§2.1, задача 5 ниже. Сервер обновлялся `deploy/site/update_site.sh`, образ
пересобран, журнал чата хранится в томе докера `smartcentr-site_site-data`.

**2. Твоя задача после одобрения Apple: переключить push в боевой режим.**
Сервер SBonus до сих пор в песочнице (`api.sandbox.push.apple.com`). Сборка,
ушедшая в App Store, ждёт боевой режим: пока `apns_production = 1` не выставлен,
покупателям не придут уведомления о заказах. Трогать **до** одобрения нельзя —
отладочные сборки на телефоне владельца живут в песочнице.

**3. Приложение на проверке у Apple (отправлено 2026-09-21).** Пока не пришёл
вердикт (1–3 дня), ничего, что меняет **вход, «Кабинет» или оформление заказа**,
на сервер не выкатывать. Проверяющий открывает живой `smarket.kg`: сломается
вход — приложение вернут по правилу 4.2, это неделя. Нужно что-то выкатить —
сначала напиши сессии, которая ведёт App Store.

**4. Кто что ведёт, чтобы не столкнуться в git.**

| Чьё | Файлы |
|---|---|
| MacBook, приложение | `ios/`, `assets-src/`, `review/`, `scripts/make-ios-icon.mjs`, `scripts/install-on-iphone.sh`, `scripts/update-all.sh`, `scripts/build-appstore.sh`, `scripts/setup-demo-login.sh`, `docs/IOS_APP_UZ.md`, `docs/APP_STORE_UZ.md`, `docs/IPHONE_TEST_UZ.md` |
| MacBook, консультант | `src/lib/assistant/`, `src/lib/telegram/`, `src/app/api/assistant/`, `src/app/api/telegram/`, `src/app/panel/`, `src/components/AssistantChat.tsx` + `assistant-chat.css`, `src/lib/store.ts`, `scripts/telegram-*`, `deploy/site/docker-compose.yml`, `Dockerfile` |
| PC | `src/` (остальное), `public/`, `integrations/`, `deploy/` (кроме compose) |
| Общее — предупреждай | `src/lib/i18n/dictionaries.ts`, `src/app/[lang]/layout.tsx`, `src/app/robots.ts`, `CLAUDE.md`, `docs/HANDOFF.md`, `docs/TODO_NEXT.md`, `graphify-out/` |

**5. Git между машинами — без изменений, но напомню.** Сначала `git status` и
`git log --oneline -10`, потом коммит **только своих** файлов, потом
`git pull --rebase origin <ветка>`, потом `git push` **без** `--force`.
Конфликт в `graphify-out/` руками не правится: возьми любую сторону,
`git add`, `git rebase --continue`, в конце `graphify update .` и отдельный коммит.

**6. Что от тебя нужно для следующего шага.** Чат не умеет отвечать на «сколько
месяцев осталось по моей рассрочке» — этих данных нет нигде, кроме 1С. Нужна
выгрузка остатка по рассрочке на телефон покупателя: сумма долга, сколько
месяцев осталось, дата ближайшего платежа. Откуда именно брать в 1С — у
владельца рассрочка ведётся по-своему, спроси его. Подробности — задача 5.2.

## ⚠ Для MacBook (написано 2026-09-21 с PC): жест «назад» и 120 Гц — нужна сборка

Владелец попросил: смахнул от левого края — страница назад, как в iPhone,
плавно и на частоте экрана. На PC правка сделана, собрать её можно только на Mac:

- `ios/App/App/BonusCard.swift` → `MainViewController.capacitorDidLoad`:
  `webView?.allowsBackForwardNavigationGestures = true` (системный жест WebKit).
- `ios/App/App/Info.plist`: `CADisableMinimumFrameDurationOnPhone = true` —
  без него анимации приложения на ProMotion ограничены 60 Гц.
- Проверить на iPhone, потом — в следующую сборку для App Store (не в ту, что
  сейчас на проверке). На первой странице жесту некуда вести — это нормально.
- В Safari на телефоне жест «назад» и так системный, сайт менять не нужно.

## Что сделали 2026-09-18 на MacBook (ветка `feature/ios-app`)

Целый вечер занимались приложением для iPhone. Подробности — в
[IOS_APP_UZ.md](IOS_APP_UZ.md), раздел 8. Коротко:

**Готово и проверено в симуляторе (iPhone 17 Pro, iOS 26.4):**

- Приложение на Capacitor 8.5.2, папка `ios/`. Имя **«S Маркет»** (магазин
  остаётся Smart Centr), Bundle ID `kg.smarket.app`, минимум iOS 16.
  Внутри открывается `https://smarket.kg` без адресной строки и кнопки «назад».
- Иконка и экран запуска в цветах сайта — `node scripts/make-ios-icon.mjs`.
- Страница «нет связи» — `ios-web/index.html`.
- **Бонусная карта** (`ios/App/App/BonusCard.swift`): QR рисует сам телефон,
  данные лежат в Keychain, на кассе экран сам выкручивает яркость.
  Проверено с полностью выключенным сервером — карта открылась.
- **Вход по Face ID** (`ios/App/App/AppLock.swift`): сайт выдаёт ключ
  (`/api/customer/native-key`), телефон держит его под замком.
- **Push-уведомления**: приложение отдаёт адрес телефона
  (`/api/push/device`), сервер шлёт «Заказ оплачен» и «Заказ готов».

**Выкачено в бой 2026-09-18 вечером:**

- Сервер: `deploy_shop.sh` прошёл. Новое — `shop_push.py`, таблица
  `shop_push_devices` (миграция 005), `h2` в `requirements.txt` (Apple принимает
  только HTTP/2). Бэкап БД: `before_shop_20260918_174145.sql.gz`.
- Сайт: `update_site.sh` прошёл. Бэкап: `/opt/smartcentr-site-backups/20260918_174606`.
- Ключ Apple записан скриптом `setup-apns.sh`: Key ID `8CH49X79SQ`,
  Team ID `434V33P6X2`, пока sandbox (приложения в App Store ещё нет).
  Сервер отвечает «ключ Apple задан: True».
- Профиль покупателя теперь отдаёт `qrCode` (код клиента в SBonus).

## Что сделали 2026-09-19 (ветка `feature/ios-app`)

**Политика конфиденциальности — готово.** Apple требует эту страницу перед
публикацией (правило 5.1.1) и сам её открывает.

- Адрес: `https://smarket.kg/ru/privacy` и `https://smarket.kg/ky/privacy`,
  без входа.
- Текст: `src/data/privacy.ts` (RU + KY). Страница:
  `src/app/[lang]/privacy/page.tsx`. Стили: `.legal-page*` в
  `src/app/globals.css`. Ссылка в подвале: `src/components/Footer.tsx`.
- Написано по факту кода: что собираем (имя, телефон, адрес, состав заказа,
  баланс SBonus, push-токен), что НЕ собираем (карта, биометрия, геопозиция,
  контакты), что лежит в самом телефоне (Keychain: бонусная карта и ключ
  Face ID), кому передаём (SBonus, 1С, O!Деньги, Telegram/WhatsApp, Apple).
- Проверено: `npx tsc --noEmit`, `npx vitest run` (63 теста), `npm run build`
  (обе страницы статические), обе версии открылись в браузере.
**Контакты магазина — владелец дал 19.09.2026.** Одно место на весь сайт:
`SHOP_CONTACT` в `src/data/privacy.ts`; читают и страница политики, и подвал.

- Телефоны (на всех WhatsApp и Telegram): `+996 557 100 505`,
  `+996 505 000 100`, `+996 551 120 009`.
- Адрес: Ошская область, Араванский район, ул. Ош-3000, 86.
- В текст подвала добавлено «Более 15 лет на рынке» (RU) и «Рынокто 15 жылдан
  ашык» (KY).
- Email магазина по-прежнему нет. Для сайта не обязателен. Для App Store
  Connect («App Review Information») владелец укажет свою личную почту.
- Выкачено на сервер 19.09.2026.

---

**Каталог без интернета (шаг 6) — готово.** Четвёртая и последняя
возможность, которую Apple требует от приложения.

- Пока связь есть, сайт отдаёт приложению «снимок» каталога:
  `src/app/api/catalog/snapshot/route.ts` (сейчас 470 товаров, 52 КБ).
  Отправляет `src/components/OfflineCatalogSync.tsx` через
  `src/lib/native/catalog.ts`, не чаще раза в 6 часов и только если каталог
  изменился (сверка по версии-отпечатку).
- Приложение кладёт снимок файлом к себе: `ios/App/App/OfflineCatalog.swift`.
  Там же экран каталога: поиск, разделы, список с ценой и наличием.
- Вход: на странице «Нет связи» появилась кнопка **«Каталог · Каталог»**
  (`ios-web/index.html`). Видна, только если снимок уже сохранён.
- На экране честно написано, что это сохранённый список и заказать нельзя,
  плюс дата снимка.
- Проверено в симуляторе (iPhone 17 Pro) с **выключенным** сервером: список
  открылся, поиск по «LG» отработал, фильтр «Холодильники» отработал,
  «Закрыть» вернуло на страницу «Нет связи».
- Выкачено на сервер 19.09.2026.
- Ограничение не наше: в каталоге из 1С сейчас нет ни цен, ни фотографий
  (задача 3 ниже), поэтому в списке видно «Цена по запросу» и одинаковый
  значок. Появятся цены и фото в 1С — появятся и здесь, код менять не нужно.

---

**Проверка на настоящем iPhone (шаг 7) — подготовлено.**

- Что проверять и как поставить приложение на телефон:
  **`docs/IPHONE_TEST_UZ.md`** — список с галочками, по-узбекски.
- Скрипт установки: `scripts/install-on-iphone.sh` (сам находит телефон,
  собирает с настоящей подписью, ставит). Телефон ищется через
  `xcodebuild -showdestinations`, а не через список USB-устройств: iPhone
  владельца привязан **по Wi-Fi** и в списке подключённых его нет.
- **19.09.2026: приложение установлено на настоящий iPhone 17 Pro Max**
  (iOS 27.0), подпись «Apple Development», профиль создался сам.
- В проект записана команда Apple: `DEVELOPMENT_TEAM = 434V33P6X2`.
  Сборка для симулятора после этого проверена — работает.
- Сайт выкачен 19.09.2026, `/api/catalog/snapshot` на smarket.kg отвечает —
  «каталог без интернета» на телефоне проверять можно.

---

**Сайт выкачен 19.09.2026 — два раза.** На живом smarket.kg теперь есть:
политика `/ru/privacy` и `/ky/privacy`, контакты и адрес в подвале,
«Более 15 лет на рынке», снимок каталога `/api/catalog/snapshot`, блок
«Быстрый вход» в кабинете. Бэкапы: `/opt/smartcentr-site-backups/20260919_110553`
и `..._111421`. Каталог при этом не изменился — как было 6 товаров, так и
осталось (задача 3).

**Блок «Быстрый вход» в «Кабинете» (19.09.2026).** До этого у Face ID **не было
никаких настроек**: ключ сохранялся сам, кнопка появлялась только когда вход
закончится, а специально проверить было нельзя — «Выйти» стирает ключ, а срок
входа 180 дней.

- Теперь в кабинете (только внутри приложения) видно «Face ID · Включён» или
  «Выключен», есть кнопки **«Проверить»** (телефон спрашивает лицо прямо
  сейчас), **«Выключить»** (стирает ключ, из кабинета не выходит) и
  **«Включить»**.
- Код: `src/components/AccountView.tsx`, стили `.account-lock*` в
  `src/components/account.css`, строки `account.lock*` в
  `src/lib/i18n/dictionaries.ts`.
- Проверено в симуляторе: вход → «Включён» → «Проверить» → «✓ Сработало» →
  «Выключить» → «Выключен» → «Включить» → снова «Включён».

---

**App Store, шаг 8 — подготовка идёт (20.09.2026).**

- Боевая сборка проверена: `.ipa` собирается, подписан сертификатом
  Apple Distribution, внутри `aps-environment = production` и
  `beta-reports-active = true`. Значит с подписью проблем нет.
  Скрипт: `scripts/build-appstore.sh` (сам проверяет, чем подписано и
  какие уведомления внутри, и останавливается, если не то).
- Что осталось и от кого — **`docs/APP_STORE_UZ.md`**: тексты и почта от
  владельца, готовые ответы для «ярлыка данных», порядок переключения
  ключа Apple в боевой режим.
- Снимки экрана делаются после выката сайта, иначе на них останется
  старый вид.

---

**WhatsApp: ссылка на заказ теперь кнопкой (20.09.2026) — ЖДЁТ ВЫКАТА PC.**

Было: в конце письма об оплате стоял длинный адрес с токеном, читать его
неудобно. Стало: кнопка «Статус заказа».

- Новый файл `integrations/sbonus-server/shop/shop_whatsapp.py`.
  Модуль рассрочки (`app/payments`) не тронут — берём оттуда только отправку
  текста.
- `shop_router.py` → `_notify_paid`: письмо покупателю уходит через него.
  Письмо администратору не менялось.
- Кнопка не прошла или письмо длиннее 900 знаков — уходит обычным текстом
  со ссылкой, как раньше. Покупатель сообщение получает в любом случае.
- Проверено на живом сервере, без установки: модуль загрузился, письмо с
  кнопкой ушло, нарочно сломанная кнопка ушла текстом, длинное письмо ушло
  текстом. Временный файл с сервера убран.
- **Выкатывает PC** вместе с демо-входом и удалением учётной записи:
  `deploy_shop.sh`.

Заодно исправлено: `SHOP_SITE_BASE_URL` на сервере был
`https://whitefitpro.com` — ссылки в письмах вели на старый домен. Теперь
`https://smarket.kg`. Бэкап: `/opt/sbonus/.env.production.bak_20260920_160233`.

---

**Чего НЕ хватает — по порядку:**

1. **1С prod всё ещё 1.4** (задача 2 ниже). Это главное и не связано с iPhone.
2. **Проверка на настоящем iPhone.** В симуляторе Face ID проходит сам собой,
   а push от Apple туда не доставить. Нужно поставить приложение на телефон
   владельца и проверить: окно Face ID, реальное уведомление о заказе.
3. **Не выяснено: что читает сканер на кассе.** Сейчас в QR зашит `qr_code`
   клиента из SBonus (`SB-XXXXXXXXXX`). Если касса ждёт другое (например
   телефон) — меняется одно место: что сервер кладёт в `qrCode`.
4. **Оформление под iOS 26 (Liquid Glass).** Владелец хочет «профессиональное»
   приложение. Договорились на «смешанный+»: нижнее меню, переходы и «Кабинет»
   переписать на Swift, а карточки товаров и оформление заказа оставить сайтом —
   тогда дизайн сайта меняется без обновления в App Store. **Не начато.**
5. ~~Каталог без интернета (шаг 6)~~ — сделано 19.09.2026, см. выше.
6. ~~Выкатить сайт~~ — сделано 19.09.2026.

**Сборка приложения на Mac** (обязательно `CODE_SIGN_IDENTITY="-"`, иначе
Keychain не работает и карта не сохраняется):

```
cd ios/App && xcodebuild -scheme App -configuration Debug -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -derivedDataPath build CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO \
  CODE_SIGNING_ALLOWED=YES
```

Проверка на локальном сайте: в `capacitor.config.ts` временно поменять `url`
на `http://127.0.0.1:3000`, сделать `npx cap sync ios`, потом вернуть обратно.

---

## Решения владельца (не переспрашивать)

- **Вход на сайт (2026-09-17):** по номеру телефона + 4-значный код в WhatsApp, без пароля. Новый номер — автоматическая регистрация в SBonus. Смотреть товары можно без входа; вход нужен для заказа и «Кабинета».
- **Приветственный бонус сайта:** 1000 сом (сумма настраивается), **только новым** клиентам (кого ещё нет в SBonus), один раз на телефон. Обычный welcome 100 сом SBonus при этом не даётся.
- **Лимит оплаты бонусами на сайте — свой процент для сайта** (настраивается владельцем, по умолчанию 10%), а не процент уровня SBonus.

- SBonus продолжает работать. Ничего в нём не ломать.
- Магазин — временно на **whitefitpro.com**, потом свой домен. Главный `smartcentr.store` не трогать.
- Интеграция с SBonus — все три пункта: **бонусы за покупки на сайте**, **оплата бонусами**, **личный кабинет на сайте**.
- Заказы из 1С загружаются каждые **5 минут** (оставить так).

---

## Задача 1. Домен магазина — ✅ сделано (временно)

- 2026-09-17: магазин работает на **https://whitefitpro.com** (временно, по решению владельца). Прежняя программа WhiteFit (порт 3000) осталась запущенной, но без адреса.
- smartcentr.store снова показывает прежнюю страницу.
- `SHOP_SITE_BASE_URL=https://whitefitpro.com` в `/opt/sbonus/.env.production`.
- Когда будет свой домен: A-запись → `145.223.100.16`, затем на сервере `bash /tmp/set_shop_domain.sh новый.домен www.новый.домен` (скрипт `deploy/site/set_shop_domain.sh`).
- Отмена: `bash /opt/smartcentr-site-backups/domain_20260917_131231/undo.sh`.
- Мелочь: у сертификата whitefitpro.com старый deploy-hook вызывает контейнер `erp-nginx`, которого нет — сертификат всё равно продлевается, но certbot пишет ошибку.

## Задача 2. Вход, приветственный бонус и бонусы SBonus — 🟡 сервер и сайт установлены, 1С prod ещё 1.4 (2026-09-17)

Что написано:
- **Сервер** `integrations/sbonus-server/shop/`: `shop_customers.py` (send-code / verify / register / профиль, списание `spend_for_order`), бонусные поля в `shop_models.py` + `003_shop_bonus_migration.sql` (колонки `bonus_spend, bonus_spent, pay_amount, bonus_earned`; настройки `SITE_WELCOME_BONUS_AMOUNT=1000`, `SITE_BONUS_MAX_PCT=10` в таблице `settings`), `shop_router.py` (заказ с бонусами, счёт O!Деньги на сумму минус бонусы, списание после оплаты, WhatsApp). `deploy_shop.sh` ставит всё это с пробным импортом и автооткатом.
- **Сайт**: `src/lib/customer/session.ts` (подписанная cookie `sc_customer`), `src/lib/customer/gateway.ts`, API `src/app/api/customer/*`, `src/components/CustomerLogin.tsx`, `src/components/AccountView.tsx`, `src/components/account.css`, кабинет `/[lang]/account`, оформление заказа — только после входа + блок бонусов, страница заказа — строки бонусов. Тесты `__tests__/customer.test.ts`. Тестовый режим: `storefront-mock` в `.claude/launch.json`, код 1234.
- **1С** (расширение 1.5.0.1): скидка бонусами по строкам заказа (`РаспределитьСкидкуБонусами`), ПКО = `pay_amount`, после проведённой РТУ — `НачислитьБонусЗаРеализацию` (POST `/1c/purchase`, чек `РТУ-<номер>`, комментарий `SBonus+ начислено`), `bonus_earned` в mark-done.

Сделано: сервер (`deploy_shop.sh`) и сайт (`install_site.sh whitefitpro.com www.whitefitpro.com`) установлены и проверены без реального телефона; 1С 1.5 — на тестовой копии.
**Осталось:** владелец ставит `manage.py install prod` (до первого заказа с бонусами!), проверяет вход новым и старым номером, делает заказ с бонусами.

Исходный порядок установки: 1) `deploy_shop.sh` на сервере; 2) сайт: `pack.ps1` → `install_site.sh whitefitpro.com`… (внимание: install_site.sh пишет nginx-конфиг для одного домена — для whitefitpro использовать `set_shop_domain.sh` после него или только пересобрать контейнер); 3) `manage.py install test` (владелец) → проверка → `install prod`.

Проверить после установки: вход новым номером (1000 бонусов, WhatsApp), вход старым номером (без 1000), заказ с бонусами (сумма O!Деньги меньше, списание `СП-SC-...` в SBonus), в 1С скидка по строкам и ПКО на деньги, после РТУ — начисление.

## Задача 2 (справка). Интеграция сайта с SBonus

### Что уже известно о SBonus (прочитано на сервере, только чтение)

- Код: `/opt/sbonus/sbonus-backend/app`.
- `services/bonus.py`, класс `BonusService(db)`:
  - `earn(customer_id, purchase_amount, branch_id, cashier_id=None, receipt_number=None, note=None, category_slug=None)` — начисление. Минимум покупки `min_purchase_for_bonus = 500` сом (иначе 400 `BONUS_BELOW_MIN_PURCHASE`). Повтор `receipt_number` → 409 `BONUS_DUPLICATE_RECEIPT`.
  - `spend(customer_id, spend_amount, purchase_amount, branch_id, cashier_id=None, note=None, receipt_number=None)` — списание. Максимум = MIN(баланс, покупка × `tier.max_spend_pct`), по умолчанию `max_spend_percent = 30`.
- `api/v1/webhook.py` (prefix `/webhook`) — готовые точки для 1С: `POST /1c/purchase`, `POST /1c/spend` (идемпотентность через `SPEND-{receipt}`), `POST /1c/refund`, `POST /1c/register`, `GET /1c/customer/{phone}` (баланс, tier, max_spend_pct; 404 если нет клиента), `GET /1c/check-spend/{phone}?purchase_amount=`. Защита `_security_check`: флаг в БД, белый список IP, HMAC тела секретом `webhook_1c_secret`.
- `utils`: `normalize_phone` → `+996XXXXXXXXX`.
- Всем операциям нужен `branch_id` (UUID филиала). Для сайта нужен свой филиал «Интернет-магазин» или существующий — **уточнить у владельца**.
- Кабинет клиента: `https://cabinet.smartcentr.store` (`CUSTOMER_CABINET_BASE_URL`). Вход: `api/v1/customer_auth.py` (prefix `/customer-auth`): `/request-link` (magic link), `/send-otp` (4-значный код + ссылка в WhatsApp), `/verify-otp`, `/self-register`.
- Ещё не прочитано: модели Customer/Tier/Transaction, `api/v1/customer.py`, `api/v1/shop.py`, `api/v1/bonus.py`, как работает `refund`.

### Как SBonus работает сейчас (прочитано 2026-09-17: БД на VPS + код расширения SBonusPlus в 1С)

- **Филиал один:** «Смарт Центр», `branch_id = 36eccf64-521e-4d37-a301-0b1fd6d10a4f` → сайт использует его.
- **Клиентов:** 1475. Операции: earn 535, spend 51, refund 186, promo 560, referral 224, campaign 37.
- **Уровни (tiers):** Bronze 1% (списание до 30%), Silver 2% (25%), Gold 3% (20%), Platinum 5% (10%), platinum 7% (5%). ⚠ Странно: Gold `min_total_kgs=15000` меньше Silver `90000`, и два Platinum (разный регистр) — сообщить владельцу, не трогать.
- **Начисление в 1С** — вручную кнопкой «Начислить бонус» в РТУ (`SB_ОбщийМодуль.НачислитьБонусЗаРеализацию`): `POST /1c/purchase`, `receipt_number = "РТУ-<номер>"`, сумма = СуммаДокумента **после скидки** (без строк «наценка») → бонус от оплаченных денег. В комментарий РТУ пишется `SBonus+ начислено`.
- **Списание в 1С** — кнопка «Списать бонус» в РТУ: лимит 30% суммы, только в день продажи, `POST /1c/spend`, `receipt_number = "СП-<номер>"`. Бонусы проводятся как **ручная скидка** по строкам товаров (`ПроцентРучнойСкидкиНаценки`, Сумма, СуммаВзаиморасчетов), пересчитывается этап оплаты, комментарий `SBonus+ скидка: N сом`. Если в комментарии уже `SBonus+ начислено` — списывать нельзя.
- **Возврат:** подписка `SB_ВозвратПриПроведении` → `ВозвратБонусаПоРеализации` → `POST /1c/refund` с `original_receipt_number = "РТУ-<номер основания>"`.
- Источник: `D:\doonni\1С Предприятие\Базы\Смарт центр база\BAS_Migration_20260825\builds\SBonusPlus\src_v4` (выгрузка 25.08; рабочая версия может немного отличаться).

### Решение (на основе того, как работает магазин)

- Филиал — «Смарт Центр». Бонусы начисляются от суммы, оплаченной деньгами. Оплата бонусами в 1С — скидкой по строкам, как на кассе.
- **Списание:** сайт показывает баланс и максимум; сервер при создании заказа пересчитывает; `spend` на сервере сразу после подтверждения оплаты O!Деньги, receipt `СП-SC-...`. В 1С загрузка заказа ставит скидку по строкам + комментарий `SBonus+ скидка: N сом`; ПКО = сумма денег.
- **Начисление:** делает 1С (наше расширение) при создании РТУ из заказа сайта: `POST /1c/purchase`, receipt `РТУ-<номер>`, сумма = СуммаДокумента, комментарий `SBonus+ начислено`. Тогда возвраты в 1С и кнопки SBonus работают как обычно. Если товара нет и РТУ не создана — бонус начислят кнопкой при отгрузке, как в магазине.

### План (сделать в пакете `app/shop` на сервере + на сайте)

**2.1 Оплата бонусами при оформлении**
- Сайт: после ввода телефона — запрос баланса (через серверную часть сайта, с подписью `SHOP_API_SECRET`). Показать «Доступно N бонусов, можно списать до M». Галочка «Списать бонусы».
- Сервер: новая точка `GET /api/v1/webhook/site/bonus/{phone}?amount=` → баланс и максимум (через BonusService, внутри сервера, без HTTP к самому себе).
- При создании заказа сервер **сам заново** считает максимум. Сумма счёта O!Деньги = итог − бонусы. В ShopOrder сохранить `bonus_spend`.
- Если бонусы покрывают всё — счёт не нужен (решить; при лимите 30% такого не будет).
- Списание (`spend`) — **только после подтверждения оплаты**, receipt = номер заказа `SC-...`. Если на момент оплаты баланс уже меньше — записать событие и уведомить админа.

**2.2 Бонусы за покупки на сайте**
- После подтверждения оплаты: `earn` с `receipt_number = SC-...`, сумма = оплачено деньгами (уточнить у владельца: от суммы денег или от полной суммы).
- Если клиента нет в SBonus — зарегистрировать (как `/1c/register`) и отправить ему приглашение в WhatsApp.
- Проверить, что 1С **не начисляет второй раз** за ту же продажу (реализация из заказа сайта не должна идти в `/1c/purchase`). Прочитать, как 1С сейчас вызывает SBonus.
- В 1С: в ПКО/заказ передать сумму, оплаченную бонусами (как скидку или отдельную оплату) — **уточнить у владельца, как бонусы проводятся в 1С на кассе сейчас**.

**2.3 Личный кабинет на сайте**
- В шапке сайта кнопка «Кабинет».
- Простой вариант: страница `/[lang]/account` — ввод телефона → код из WhatsApp (`/customer-auth/send-otp`, `/verify-otp`) → показ баланса, уровня, истории бонусов и заказов с сайта. Либо ссылка на `https://cabinet.smartcentr.store`.
- Рекомендация: сначала ссылка на готовый кабинет + баланс на странице оформления; полноценный кабинет — вторым шагом.

**2.4 Выкатка**
- Новый код — только в `integrations/sbonus-server/shop/`. Установка через обновлённый `deploy_shop.sh` (бэкап БД, пробный импорт, автооткат).
- Миграция `003_shop_bonus_migration.sql`: поля `bonus_spend`, `bonus_earned`, статусы операций в `shop_orders`.
- Тесты на сайте (`__tests__/orders.test.ts`) для расчёта суммы с бонусами.
- Проверка реальной покупкой на 500+ сом (минимум для начисления) или временный тестовый клиент.

## Задача 3. Мало товаров в каталоге

- 2026-09-17 вечер: 1С (рабочая) отправляет **5 товаров**; фото есть только у LG F2V5PS2S. Фото не доходили, потому что в 1С сохранён не JPEG — сервер теперь переводит PNG/WebP/BMP/GIF в JPEG (коммит 1836de3), фото на сайте появилось.
- Спросить владельца: показывать все товары со склада или только настроенные; разобраться, почему запрос `ТоварыДляСайта` отдаёт 5 (условие `Остатки.ВНаличии > 0 ИЛИ есть запись в ИМ_ТоварыСайта`).
- Предложено (ждёт ответа): брать обычную картинку из карточки номенклатуры, если в «Онлайн магазине» фото нет.
- Идея на будущее от владельца: приложение для App Store (рекомендовано Capacitor + push + QR-карта SBonus; нужен Apple Developer $99/год и Xcode на MacBook).

- В каталоге на сервере 1–2 товара, остатки 0. В выгрузке из рабочей базы раньше было 2412 товаров, 470 в наличии.
- Попросить владельца в **рабочей** 1С: «Онлайн магазин» → «Настройки заказов» → «Отправить каталог» и прислать текст сообщения.
- Проверить `ТоварыДляСайта` в `src/ЗаказыСайтаСервер.bsl`: какие товары попадают (только отмеченные в регистре `ИМ_ТоварыСайта`? остатки по складу из настроек?).
- Проверить, с какой базы работает регламентное задание (тестовая копия тоже шлёт каталог и может перезаписывать рабочий).

## Задача 4. Сохранить работу в GitHub — ✅ коммиты 0e2fa54 и следующий (вход и бонусы)

- Дальше коммитить по просьбе владельца. Не коммитить: `.env.local`, `smartcentr-site.tar.gz`, `scripts/__pycache__/`, `review/_tmp-*`, папки `.agent/ .agents/ .claude-install/ .codex/ .cursor/ .gemini/ .zcode/` (спросить).

## Задача 5. Онлайн-консультант (чат на сайте) — сделан локально, ждёт двух вещей

Сделано (ветка `feature/ios-app`, MacBook): чат в углу сайта,
`src/components/AssistantChat.tsx` и `src/lib/assistant/`. Отвечает по каталогу
на русском, кыргызском и узбекском; узнаёт вошедшего покупателя по имени,
называет его бонусы и его заказы. Без ключа Gemini работает запасным режимом:
поиск по каталогу и телефон магазина.

**5.1 Ключ Gemini.** Владелец берёт бесплатный ключ на
`https://aistudio.google.com/apikey` и вписывает `GEMINI_API_KEY` в `.env.local`
(на сервере — в окружение контейнера `smartcentr_site`). Без него чат живой, но
отвечает шаблонами, а не человеческим текстом.

**5.1а Что ещё сделано в чате (20.09.2026).**

- **«Написать сотруднику»** — строка под перепиской. Открывает WhatsApp, в
  сообщение уже вписаны вопросы покупателя, товары, которые ему показали, и
  адрес страницы. Сотруднику не надо переспрашивать «а что вы смотрели».
- **Журнал вопросов** — `src/lib/assistant/log.ts`. Простой текстовый файл,
  новый на каждый месяц, лежит в папке из `ASSISTANT_LOG_DIR` (по умолчанию
  `data/`). Телефоны и длинные цепочки цифр в тексте заменяются точками.
- **Страница для владельца** — `/panel/questions?key=…`, ключ в
  `ASSISTANT_LOG_KEY`. Видно: сколько вопросов по дням, что спрашивают чаще
  всего и — главное — **на что товар не нашёлся**. Без ключа страница отдаёт
  404, в поиск не попадает.
- **Телеграм-бот** — тот же «мозг». Боевой режим: `/api/telegram/webhook` +
  `scripts/telegram-webhook.sh` (включает webhook у Telegram). Для проверки на
  своём компьютере: `node scripts/telegram-bot.mjs` — опрашивает Telegram сам,
  публичный адрес не нужен.

**Осталось по этим пунктам:**

- На сервере журналу нужна подключённая папка, иначе он стирается при каждом
  обновлении сайта. В `deploy/site/docker-compose.yml` добавить сервису `site`:
  `volumes: [ "./data:/app/data" ]` и `ASSISTANT_LOG_DIR=/app/data` в
  `.env.production`. Это половина PC — трогать с MacBook не стали.
- Журнал вопросов в «Панели сайта» 1С (вместо отдельной страницы) — отдельная
  задача: нужна таблица на сервере SBonus и запрос из 1С.

**5.1б Каталог прямо из 1С (сделано, ждёт двух строк настроек).**

Чат берёт цены и наличие с сервера заказов — того самого, куда 1С шлёт каталог
каждые 10 минут (`src/lib/assistant/live.ts`). Обновляется раз в 10 минут, в
памяти; сервер не ответил — работает по каталогу, вшитому при сборке сайта.

Чтобы включить, в `.env.local` (на сервере — `.env.production`) должны быть
заполнены **SHOP_API_URL** и **SHOP_API_SECRET**. Это те же настройки, по
которым сайт создаёт заказы, ничего нового заводить не нужно. Пока они пустые,
чат отвечает по вшитому каталогу — это видно в журнале сервера строкой
`[assistant] каталог обновлён из 1С: N товаров`.

**5.1в Пределы, чтобы чат не съели (сделано).** `src/lib/assistant/limits.ts`:
не больше 20 сообщений от одного человека за 10 минут и не больше
`ASSISTANT_DAILY_LIMIT` (по умолчанию 1500) ответов модели в сутки. Предел
выбран — чат не молчит, а отвечает поиском по каталогу и телефоном магазина.

**5.1г Проверка качества (20.09.2026).** Прогнали 35 живых вопросов на трёх
языках. Нашли и исправили семь ошибок: чат отвечал «гарантия 0 месяцев» (в 1С
срок не заполнен), выдумывал часы работы, обещал возврат денег, приписывал
стиральной машине инверторный мотор, называл покупателю «1С», показывал
микроволновку под ответом про стиральные машины и терял язык разговора на
коротких вопросах вроде «qachan keladi?».

Главное правило в `src/lib/assistant/prompt.ts` теперь стоит первым: **не
выдумывать ничего**, чего нет в тексте подсказки. Проверять после каждой
правки промпта стоит теми же вопросами — «kafolati necha yil?», «до скольки
работаете?», «деньги вернёте?», «A+++ klassmi?».

**5.1д Знания для чата и каталог «по вопросу» — 🟡 написано на PC 2026-09-21, не выкачено.**

- **Знания от владельца.** 1С → «Онлайн магазин → Панель сайта» → поле «Знания для
  чата»: часы работы, гарантия, возврат, акции обычными словами; сохраняется кнопкой
  «Сохранить настройки». Сервер: таблица `shop_assistant_notes` (миграция 007),
  `GET/POST /webhook/1c/shop/notes`, `GET /webhook/site/notes` в `shop_admin.py`.
  Сайт: `src/lib/assistant/notes.ts` (раз в 10 минут), блок «ЗНАНИЯ ОТ ВЛАДЕЛЬЦА» в
  `prompt.ts`. Если с сервера не пришли, 1С поле не отправляет — пустое не затрёт текст.
- **Каталог по вопросу.** Вместо «первые 400 товаров по 4 характеристики»:
  `catalogForQuestion` в `knowledge.ts` — до 25 товаров по трём последним вопросам
  целиком (все характеристики и описание), остальной каталог одной строкой.
- Работает и в Telegram-боте: тот же `answer()`.

**5.2 Рассрочка — 🟡 написано на PC 2026-09-21, не выкачено.** Источник в 1С —
`РегистрСведений.ГрафикПлатежей` (тот же, что «Журнал платежей → К поступлению» и
касса SBonus). 1С: `ОтправитьРассрочкуНаСайт` в `ЗаказыСайтаСервер.bsl`, идёт тем же
заданием, что каталог. Сервер: `shop_installments.py` + `shop_installments_calc.py`,
таблица `shop_installments` (миграция 006). Сайт: `getInstallment` в
`src/lib/customer/gateway.ts` — только по телефону из cookie; чат: `prompt.ts`,
`local.ts`. Проверка на копии 1С: `python scripts/1c-export/check_installments.py`.
Выкат: 1С `install test` → проверка → `install prod`; сервер `deploy_shop.sh`
(заодно довозит `shop_whatsapp.py` — его не было в списке файлов); сайт
`update_site.sh`. Порядок: сервер → сайт → 1С prod.

Было так:
**5.2 Рассрочка — чего сейчас нет.** Владелец просил, чтобы покупатель мог
спросить «сколько месяцев осталось по моей рассрочке». Этих данных нет нигде за
пределами 1С: сайт их не получает, SBonus не хранит. Консультант сейчас честно
отвечает «в чате этой цифры нет» и даёт телефон — выдумывать месяцы ему прямо
запрещено (`src/lib/assistant/prompt.ts`, блок РАССРОЧКА).

Чтобы это заработало, нужна цепочка (это половина PC, не MacBook):

1. **1С**, расширение `ИМ_ОнлайнМагазин`: выгрузка остатка по рассрочке на
   телефон покупателя — сумма долга, сколько месяцев осталось, дата ближайшего
   платежа. Откуда именно брать (договор, график платежей, регистр
   взаиморасчётов) — **спросить владельца**, у него рассрочка ведётся по-своему.
2. **Сервер SBonus**, `app/shop`: принимать эту выгрузку и отдавать по
   подписанному запросу `/api/v1/webhook/site/customer/<телефон>/installment`.
3. **Сайт**: добавить поле в `CustomerBrief` (`src/lib/assistant/knowledge.ts`),
   заполнять его в `src/app/api/assistant/route.ts` и снять запрет в
   `prompt.ts` и `local.ts`.

Порядок важен: пока шага 1 нет, шаги 2 и 3 делать нечего.

## Мелочи

- `deploy_shop.sh`: команда «ОТКАТ» удаляет заказы — переписать откат, чтобы он не трогал таблицы с данными.
